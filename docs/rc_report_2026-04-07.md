# CloudMem RC Report (2026-04-07)

## Verdict
- **RC Status:** ✅ Ready for RC
- **Decision Date:** 2026-04-07

## Scope included
- P0: session finalizer fail-closed + CLI non-zero on failure
- P0: MCP read-path structured errors (no silent-empty on read failure)
- P0: failure-path tests for MCP read tools
- P1: hook state path migration to `~/.cloudmem/hook_state` with legacy fallback
- P1: onboarding CLI flag passthrough + tests
- P1: README consistency and platform support notes
- P2: release checklist documentation

## Validation executed
1. Full test suite
   - Command: `.venv/bin/python -m pytest -q tests/`
   - Result: `34 passed`
2. Node installer syntax
   - Command: `node --check bin/install.mjs`
   - Result: pass
3. CLI smoke
   - Command: `.venv/bin/python -m cloudmem --help`
   - Result: pass
4. Onboarding CLI smoke
   - Command: `.venv/bin/python -m cloudmem onboard --help`
   - Result: pass

## Known non-blocking note
- `chromadb` emits a deprecation warning under Python 3.14 (`asyncio.iscoroutinefunction` deprecation).
- Current CI target is Python 3.11, so this is non-blocking for this RC.

## Release gate before final ship
- [ ] Run CI on target branch (GitHub Actions green)
- [ ] Execute `docs/release_checklist.md` manually once on clean environment
- [ ] Confirm hook migration behavior on an existing user setup (legacy state present)
- [ ] Tag release and publish notes
